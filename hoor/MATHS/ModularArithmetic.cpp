#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // (a+b)%m = (a%m + b%m)%m

    //(a-b)%m = ((a%m - b%m)%m + m)%m

    //(a*b)%m = ((a%m) * (b%m))%m //Focus on Parenthesis

    return 0;
}